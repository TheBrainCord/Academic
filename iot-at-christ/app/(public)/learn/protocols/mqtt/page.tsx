import { ProtocolLab } from '@/components/protocols'

export const metadata = { title: 'MQTT Packet Journey | IoT at CHRIST', description: 'Deterministic MQTT packet journey for classroom teaching and revision.' }

export default function MqttProtocolLabPage() { return <ProtocolLab /> }
