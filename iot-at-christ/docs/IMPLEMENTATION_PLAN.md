# Interactive learning studios — implementation plan

This slice integrates the independently merged hardware and protocol work into the public learning journey.

1. Expose the complete, verified LED lesson through the hardware lesson route.
2. Provide board-specific wiring, progressive assembly, conceptual current flow, fault injection, code-to-hardware explanation, assessment and evidence prompts.
3. Expose the deterministic MQTT packet journey through a dedicated public route.
4. Keep educational data separate from interaction components and preserve the earlier sense–think–act lesson.
5. Verify unit tests, type checking and the production build; document remaining limitations.

Next: add complete DHT22/BME280 lessons, connect the authored hardware schema to the free-form workbench validator, and add CoAP and HTTP packet journeys.
